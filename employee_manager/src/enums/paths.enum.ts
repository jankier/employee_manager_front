export enum Paths {
  LOGIN = 'login',
  DASHBOARD = 'dashboard',
  EMPLOYEES = 'employees',
  EMPLOYEE = 'employee',
  PROJECT = 'projects',
  CREATE = 'create',
  CHANGE_PASSWORD = 'change-password',
}
